import mongoose from 'mongoose';
const schema = new mongoose.Schema({ name: { type: String, required: true, trim: true }, type: { type: String, enum: ['client', 'internal'], default: 'internal' }, clientName: String, status: { type: String, enum: ['not_started', 'in_progress', 'on_hold', 'completed', 'cancelled'], default: 'not_started' }, priority: { type: String, enum: ['low', 'medium', 'high', 'urgent'], default: 'medium' }, progress: { type: Number, min: 0, max: 100, default: 0 }, startDate: Date, dueDate: Date, assignedTo: [String], description: String, tags: [String], budget: Number, createdBy: String }, { timestamps: { createdAt: 'createdAt', updatedAt: 'lastUpdated' }, collection: 'internal_projects' });
export default mongoose.models.InternalProject || mongoose.model('InternalProject', schema);
